name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Install deps
        run: pip install -r app/requirements.txt

      - name: Run tests
        run: pytest

      - name: Build Docker
        run: docker build -t chatbot .

      - name: Push & Deploy (stub)
        run: echo "deploy here"